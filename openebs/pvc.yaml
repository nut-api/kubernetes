kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: local-hostpath-pvc
spec:
  storageClassName: dell-2-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5G
